import QtQuick 2.15
import QtQuick.Layouts
import QtQuick.Controls
import "../components"

Item {
    id: root
    anchors.fill: parent

    property int activeColors: 6
    property int Accuracy: 100

    ColumnLayout {
        anchors.fill: parent
        spacing: 30
        Layout.margins: 50

        RowLayout {
            spacing: 50

        }
    }

}
